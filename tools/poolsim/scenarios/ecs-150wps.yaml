# ECS 150 WPS Configuration
#
# Simulates a production ECS deployment for 150 WPS workloads with reservoir mode.
#
# Services:
# - 8 History × 100 connections = 800
# - 6 Matching × 100 connections = 600
# - 4 Frontend × 100 connections = 400
# - 2 Worker × 100 connections = 200
# Total: 2,000 connections
#
# Expected convergence: ~20 seconds (2000 / 100 = 20s)

seed: 42
endpoint: cluster.dsql.eu-west-1.on.aws

duration: 30m
sampleEvery: 1s

limiterPerSec: 100

assertMaxConnectsPerSec: 100
assertConvergeWithin: 60s
assertStableFor: 5s
assertZeroEmptyEvents: true

scenario:
  kind: cold_start

services:
  # History (8 replicas)
  - name: history-1
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 0ms

  - name: history-2
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 50ms

  - name: history-3
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 100ms

  - name: history-4
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 150ms

  - name: history-5
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 200ms

  - name: history-6
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 250ms

  - name: history-7
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 300ms

  - name: history-8
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 350ms

  # Matching (6 replicas)
  - name: matching-1
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 400ms

  - name: matching-2
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 450ms

  - name: matching-3
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 500ms

  - name: matching-4
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 550ms

  - name: matching-5
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 600ms

  - name: matching-6
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 650ms

  # Frontend (4 replicas)
  - name: frontend-1
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 700ms

  - name: frontend-2
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 750ms

  - name: frontend-3
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 800ms

  - name: frontend-4
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 850ms

  # Worker (2 replicas)
  - name: worker-1
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 900ms

  - name: worker-2
    targetReady: 100
    guardWindow: 45s
    baseLifetime: 11m
    jitter: 2m
    startDelay: 950ms
